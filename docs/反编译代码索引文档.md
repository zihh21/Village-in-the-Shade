# 反编译代码索引文档

> 说明：本文档是《Village in the Shade》（日本一/NIS，Steam #3934250）反编译分析的总索引。
> 更新原则：每获取新的反编译信息后必须同步补充本文件；各模块的详细分析单独成文并在此登记链接。

## 1. 项目概述

- **游戏**：Village in the Shade（原生 C++ 引擎 Nmpl/Ngf，内嵌 Lua 5.4 脚本引擎）。
- **主程序**：`village.exe`，PE32+，ImageBase `0x140000000`，编译期默认关闭 ASLR 重定位（基址通常固定）。
- **段映射（rva ↔ 文件偏移）**：
  - `.text`：VA `0x1000` / roff `0x400`，rva − 文件偏移 = `0xC00`
  - `.rdata`：VA `0x9FC000` / roff `0x9FA800`，rva − 文件偏移 = `0x1800`
  - `.data`：VA `0x1004000` / roff `0x1002800`，rva − 文件偏移 = `0x1800`
  - `.pdata`：rva `0x3189000` / foff `0x10D1600`，共计 **31561 个函数**（PE 异常目录）
- **工具链**：见 `docs/` 同目录说明与 `D:\code\.tools\Native\NIS\` 下的 Python 脚本（capstone 反汇编族）。

## 2. 反编译产物与工具链索引

| 资源 | 位置 | 说明 |
|---|---|---|
| 反汇编主工具 | `D:\code\.tools\Native\NIS\disasm.py` | 任意 rva/foff + 长度反汇编，`-r` 解析 rip 相对目标并读字符串 |
| 字符串交叉引用 | `strxref.py` / `strscan.py` / `strs_in_range.py` | 定位字符串、扫描段内字符串 |
| 调用/立即数/链 xref | `callxref.py` / `movxref.py` / `qwordxref.py` | 函数调用点、mov imm、qword 引用分析 |
| 修饰名/RTTI 分析 | `mangled_xref.py` / `rtti*.py` / `rtti_dump.py` | `.data` 中 RTTI 类型描述符（本引擎大部分"函数名"来自 RTTI lambda 闭包描述符） |
| PE 解析 | `peinfo.py` / `pe_map.ps1` | 节表、`.pdata`、ImageBase |
| 转储/单步输出 | `dump.py` / `pdump.py`（按 `.pdata` 函数边界） | Hook 点所在函数的精确切片 |
| 数据文件提取 | `FafullfsTool\`、`extract\`、`decompiled\` | `data.dat`（FAFULLFS）全量提取；`unluac.jar` 反编译 Lua（178/192 个脚本） |

**重要反汇编切片输出**（`out_*.txt`）：

| 文件 | 内容 |
|---|---|
| `out_388B30.txt` | `CTask_Menu_Fishing` 构造函数/注册（含默认值、update 回调注册） |
| `out_38A860.txt` | 节奏判定 `update` 入口：输入 0x55a 查询、命中路径、SE/UI 事件 |
| `out_38ABE0.txt`、`out_38AF0A.txt`、`out_38B100.txt`、`out_38B2C0.txt` | 命中/漏击辅助、音符激活循环、成功路径（0x38B2E6→0x38B343） |
| `out_fishstr.txt` / `out_funcstrs.txt` | 钓鱼代码区函数→字符串指纹表（定位依据） |
| `out_singleton.txt` | 全局单例对象（0x10E7B78 等）分析 |
| `stub_dump.bin` / `stub_dump2.bin` | `src/FishingAutoCatch` 生成的 Hook stub 反汇编调试用 |

## 3. 关键地址速查表（rva，ImageBase 0x140000000）

### 钓鱼节奏小游戏（详见「钓鱼节奏小游戏分析.md」）

| 地址 | 说明 |
|---|---|
| `0x38A860` | **Hook 点**：`CTask_Menu_Fishing::update` 节奏判定函数（含输入、命中/漏击、成功检查） |
| `0x388B30` | 任务构造函数：默认值（+0x218/21c/220=0x42480000/0x433e0000/0x43480000；+0x224=0x40c00000；+0x228=0x42700000；+0x22c=0x40a00000），尾部注册 update 回调 |
| `0x388DD0` | 注册用的独立 stub（`jmp 0x38A860`） |
| `0x388DE0` | 析构函数（释放 +0x250 音符数组及 0x240/0x238/0x230 对象） |
| `0x388F50` | 音符/界面 builder（唯一含 `fishing_base`/`notesBase`/`note%02d`/`pos01`/`bar` 字符串） |
| `0x38B640` | 每次音符结果后的辅助（判定计数/反馈，失败时复位完成标志） |
| `0x7ef810` | 输入查询（edx=0x55a） |
| `0x7490e0` / `0x7491b0` | 时间源（当前时间） / 小游戏推进判定 |
| `0x2cb5b0` / `0x79f810` / `0x79f8e0` | message 构造 / 取对象 / 查对象（UI 反馈用） |
| `0x748d50` | 消息发送（edx=消息 id） |
| `0x75abb0` / `0x75ad80` | 事件发送 / SE 播放（全局单例对象 0x10E7B78） |
| `0x10E7B78` | 全局单例对象（.data，事件/SE 投递目标） |

### 钓鱼相关注册对象（RTTI 描述符定位）

| foff | 含义 |
|---|---|
| `0x1087680` | `<?AV<lambda_1>@?EH@??update@CTask_Menu_Fishing@@...>`（update 内 lambda 描述符） |
| `0x10877A0` 区 | `failureProcess` 相关描述符 |
| `0x1087770` | `CClassRegister<CTask_Menu_Fishing>` |
| `0x10877D0` | `CClassIDRegister`（含 `fishing_base`） |
| `0x1068F00` / `0x1068F90` | `CState_Player_FishingRod` 相关注册 |
| `0x100DFD0` / `0x100E040` / `0x100E0B0` | `checkFishingEnable` / `update_Inner` / `initialize_Inner` |
| `0xE33C20`–`0xE33C98` | ClassID::name 字符串区（`fishing_base` rva 0xE35470、`notesBase` 0xE35480、`note%02d` 0xE35490） |
| `0xE1384C` / `0xE28EAC` / `0xE28EA4` | 字符串 `pos01` / `pos10` / `pos11`（音符锚点） |
| `0xE33E80` 区 | 消息/事件注册表（含 `{0x140388F50, 0x140050860}`） |

### 对象关键偏移（CTask_Menu_Fishing）

| 偏移 | 类型 | 含义 |
|---|---|---|
| +0x1a8 | ptr | 输入代理（0x7ef810 的 rcx） |
| +0x218/21c/220 | float | 位置类默认值 |
| +0x224 / +0x22c | float | 判定窗口前后沿（0x40c00000=6.0 / 0x40a00000=5.0） |
| +0x228 | float | 音符激活提前量（0x42700000=60.0） |
| +0x230 / +0x238 | ptr | 任务/上下文对象 |
| +0x240 | ptr | 场景对象；`[[+0x240]+0x20]+0x568 == 0x3F2` ⇒ 咬钩小游戏运行中 |
| +0x248 | byte | 完成标志（0=进行中，成功路径 0x38B310 置 1） |
| +0x250 | ptr | 音符数组起始（元素 8 字节 `{float 时间; int 状态}`） |
| +0x258 | ptr | 音符数组结束（总数 = (结束−起始)>>3） |
| +0x268 | dword | 当前判定索引（每次处理后 0x38B2E0 +1） |

## 4. 模块文档登记

| 文档 | 范围 |
|---|---|
| `docs/钓鱼节奏小游戏分析.md` | CTask_Menu_Fishing 对象模型、音符状态机、成功/失败路径、消息语义、Hook 设计 |
| （待补） | 玩家钓鱼状态机 CState_Player_FishingRod、浮标/收杆流程 |
| （待补） | 数据格式：FAFULLFS / Lua 脚本结构、Lua 与 C++ 交互 |
| （待补） | 引擎消息/事件框架：0x7ef810/0x748d50 输入与消息路由 |

## 5. 约束与约定

- 不直接修改游戏本体文件；所有 Mod 以**运行时内存 Hook**（inline detour）实现。
- Mod 代码在 `src/<ModName>/`，按语义拆多个 `.cs`（每文件 ≤500 行），版本在 `src/<ModName>/version.txt`。
- 反汇编验证流程：修改 Hook stub 后必须用 `--dump-stub` + capstone 逐指令核对（本项目已如此验证）。
- 函数名优先来源：RTTI lambda 描述符 / ClassID::name；`.data` 中 `0x100xxxx` 的修饰名多为闭包 TypeDescriptor（qword/lea 引用数为 0），不可作为 xref 依据。
- 被替换的 prologue 必须完整复放并跳回（本 Hook 覆盖 15 字节，回跳点在 `target+15`）。