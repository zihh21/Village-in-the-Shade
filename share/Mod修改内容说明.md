# Mod 修改内容说明

> 本文件由人工维护，随每次 Mod 功能新增/调整同步更新；不参与分享包自动生成。

## 当前包含的 Mod

### 1. FishingAutoCatch 钓鱼自动收杆 `v0.1.0`

| 项目 | 内容 |
|---|---|
| 目录 | `src/FishingAutoCatch/`（C# 加载器，net8.0/x64） |
| 生效范围 | 全局（任意地点钓鱼均生效） |
| 热键 | **F8**：开启/关闭；注入后默认开启 |
| 用法 | 先启动《Village in the Shade》并进入标题画面后，运行 `Mods/FishingAutoCatch/FishingAutoCatch.exe`（或 `--status` 查询、`--remove` 还原）；重启游戏后需重新注入 |

**功能**：

- 跳过「鱼上钩后拉线节奏小游戏」（CTask_Menu_Fishing 的节奏判定）：
  开启状态下，咬钩进入节奏小游戏后，把全部音符置为"命中"并把判定索引置为音符总数，
  同一帧即自然走到游戏的**正规成功路径**（全部音符命中检查 → 发成功事件 + 消息 0xFB）。
- 与手动打完所有音符完全同一条结算路径：**不修改鱼品质、数量与奖励规则**。
- 保留原流程：**甩竿 → 等咬钩 → 收杆** 的节奏完全不变，仅跳过咬钩后的按键节奏环节。
- 关闭状态下（F8）游戏行为与未装 Mod 完全一致。

**技术要点**：

- 反编译依据：`village.exe` rva 0x38A860（CTask_Menu_Fishing::update 判定函数）、成功路径 0x38B2E6→0x38B343、
  汇合点 0x38B114。详见 `docs/钓鱼节奏小游戏分析.md`。
- 实现方式：纯 C# 加载器 + 15 字节 inline hook（`jmp` 到注入内存的 230 字节 stub）。
- Mod 结构：`Program.cs`（入口/CLI）、`Injector.cs`（分配内存/打补丁/还原/校验）、
  `HookStub.cs`（机器码生成器，含对象偏移与反编译注释）、`ProcessManager.cs`（定位进程）、
  `PatchState.cs`（注入状态持久化）、`Win32Api.cs`（P/Invoke）。

## 更新记录

### 2026-09-23（首次初始化）

- **新增** FishingAutoCatch `v0.1.0`：完成《Village in the Shade》反编译初始化
  （工具链、FAFULLFS 提取、Lua 反编译、钓鱼系统代码区定位与完整判定路径分析）。
- **新增** 钓鱼自动收杆功能：跳过咬钩后节奏小游戏并走正规成功结算；热键 F8 全局开关。
- **新增** 文档：`docs/反编译代码索引文档.md`、`docs/钓鱼节奏小游戏分析.md`。
- **说明**：本作为原生 C++ 引擎（非 Unity），按用户具体要求优先实现钓鱼 Mod；
  通用模板中的 F1 调试面板/货币修改不适用（无 OnGUI/IMGUI 运行环境），后续如需可另行实现。